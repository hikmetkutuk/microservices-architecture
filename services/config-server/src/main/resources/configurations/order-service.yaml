spring:
  datasource:
    username: root
    url: jdbc:postgresql://localhost:5433/order
    password: order
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: 'true'
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: POSTGRESQL

server:
  port: 8093

application:
  config:
    customer-url: http://localhost:8091/api/v1/customer
    product-url: http://localhost:8092/api/v1/product